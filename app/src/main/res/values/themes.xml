<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Plantory" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Plantory" parent="Base.Theme.Plantory">
        <item name="android:datePickerDialogTheme">@style/PlantDatePickerDialogTheme</item>
    </style>

    <style name="PlantDatePickerDialogTheme" parent="android:Theme.Material.Light.Dialog">
        <item name="colorOnPrimary">@color/gray_800</item>
        <item name="android:datePickerStyle">@style/PlantDatePickerStyle</item>
    </style>

    <style name="PlantDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <item name="android:headerBackground">@color/mint</item>
    </style>


    <style name="Theme.Plantory.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/mint</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.Plantory</item>
    </style>

</resources>